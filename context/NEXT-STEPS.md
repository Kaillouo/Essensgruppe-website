# Next Steps

**Last Updated:** 2026-02-28

<!-- User: write your priorities here between sessions. Claude reads this at session start. -->

## Current Priorities

1. **Merge feature/german-translation → main** — commit all pending changes, merge branch, deploy to OCI
2. **Mobile optimization** — test all pages on mobile, fix responsive issues
3. **Forum bubble/graph view** — Obsidian-style force-directed graph (see features/forum-bubbles.md)

## Backlog

- **Chat AI character** — fill in `context/features/ai-chatbot-character.md`, set `AI_SYSTEM_PROMPT` in backend `.env`, add `ANTHROPIC_API_KEY` to backend `.env`
- BlueMap nginx reverse proxy on OCI (`/bluemap` → localhost:8100)
- Stundenplan component (waiting for schedule data)
- MC Leaderboard (waiting for data source)
- Loading states for all async operations
- Error handling improvements
- Admin analytics dashboard enhancements
